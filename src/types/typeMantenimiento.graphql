type Mantenimiento {
    id: ID!
    descripcion: String!
    grupo: GrupoMantenimiento!
    createdAt: String!
    updatedAt: String!
}

type Query{
    AllMantenimiento: [Mantenimiento!]
    MantenimientoById(id: ID!): Mantenimiento
}